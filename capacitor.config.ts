import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lucasbarroca.myfavpoke',
  appName: 'MyFavPoke',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
